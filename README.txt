Antigravity Prototype Folder

Files:
- index.html
  Standalone HTML/CSS/JS prototype (progress bar + TOC scroll-spy + sticky CTA).
  Open directly in a browser or serve as static files.

- MobileArticlePrototype.jsx
  React + Tailwind component version. Tailwind must already be configured in your app.

Quick run:
- HTML: open index.html
- React: import MobileArticlePrototype.jsx and render <MobileArticlePrototype />
